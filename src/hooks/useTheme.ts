// Re-export from theme so both import paths work:
//   import { useTheme } from '../../hooks/useTheme';
//   import { useTheme } from '../../theme/useTheme';
export { useTheme } from '../theme/useTheme';
